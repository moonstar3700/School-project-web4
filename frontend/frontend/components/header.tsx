import Link from "next/link";

const Header : React.FC = () => {
    return (
        <header>
            <div>
                <a href="/">News Capture</a>
            </div>
        </header>
    )
}

export default Header